# Public base URL used for Open Graph / canonical metadata.
# On Railway this is provided automatically; set it locally if you want
# absolute OG URLs during development.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
